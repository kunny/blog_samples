language: android

android:
  components:
    - build-tools-19.0.3
    - build-tools-19.0.0

env:
  # TERM=dumb : For Gradle build output
  - TERM=dumb

script:
  # Build all gradle-based Android projects
  - cd Android
  - ./build.sh